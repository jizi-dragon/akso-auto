# 配置蓝图模板

> 使用说明：本模板用于 AKSO eGMP 平台新模块配置前的系统化分析。参照 `AKSO_API_LIFECYCLE_CONFIG.md` 格式填写。  
> 填写完成后，将此文件交给 akso-basic-config（DOM）或 akso-basic-config-api（API）执行 Skill 进行自动化配置。  
> 模板位置：`akso-gmp/playbooks/blueprint-template.md`

---

## 一、配置方案总览

### 1.1 设计目标

<!-- 用 3-5 个要点描述本配置方案要实现的业务目标 -->

- 目标1：
- 目标2：
- 目标3：

### 1.2 配置对象清单

| 序号 | 对象名称 | 对象编码 | 类型 | 生命周期 | 电子签名 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | | `xxx__c` | 主对象 | ✓/✗ | ✓/✗ | |
| 2 | | `xxx__c` | 子对象 | ✓/✗ | ✓/✗ | |

### 1.3 模块菜单结构（业务端）

```
一级菜单名称
├── 子菜单1
├── 子菜单2
│   ├── 二级子菜单2.1
│   └── 二级子菜单2.2
└── 子菜单3
```

---

## 二、主对象：{对象中文名}（`{object_code}__c`）

### 2.1 字段定义

#### 基本信息

| 字段名称 | 字段编码 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- | --- |
| 编号 | `name__ak` | 文本（标准） | 自动 | 自动编号 |
| | `xxx__c` | 文本 | ✓ | |
| | `xxx__c` | 下拉 | ✓ | |

#### 业务信息

| 字段名称 | 字段编码 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- | --- |
| | `xxx__c` | | | |

#### 时间信息

| 字段名称 | 字段编码 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- | --- |
| | `xxx__c` | 日期 | | |

#### 关联信息

| 字段名称 | 字段编码 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- | --- |
| | `xxx__c` | 对象引用 | | |

#### 附件与记录

| 字段名称 | 字段编码 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- | --- |
| | `xxx__c` | 附件 | | |

### 2.2 生命周期设计

生命周期名称：{对象中文名}生命周期

状态定义：

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  状态1   │───▶│  状态2   │───▶│  状态3   │───▶│  状态4   │
│          │    │          │    │          │    │          │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
```

状态流转规则：
- 状态1 → 状态2：条件 = {条件描述}
- 状态2 → 状态3：条件 = {条件描述}

状态权限：
- {状态名} → {状态名} 的流转：{角色限制}
- {特殊操作}：{角色限制}

### 2.3 编号规则

| 规则名称 | 编号模式 | 示例 |
| --- | --- | --- |
| {对象名}编号 | PFX('{前缀}') + DATE(YYYYMM) + SEQ({位数}) | {示例} |

### 2.4 选项集定义

| 选项集名称 | 选项集编码 | 选项值（显示值,存储值） |
| --- | --- | --- |
| | `option_xxx` | 选项A,`option_a` / 选项B,`option_b` |

---

## 三、子对象：{子对象中文名}（`{object_code}__c`）

### 3.1 字段定义

<!-- 重复 2.1 的字段表结构 -->

| 字段名称 | 字段编码 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- | --- |
| | `xxx__c` | | | |

### 3.2 生命周期设计

<!-- 如有生命周期，重复 2.2 的结构；如无则删除此节 -->

### 3.3 编号规则

<!-- 重复 2.3 的结构 -->

---

<!-- 继续复制第三章的结构，为每个子对象添加独立的章节 -->

---

## 四、工作流设计

### 4.1 {对象名}审批流程

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  节点1      │────▶│  节点2      │────▶│  节点3      │
│  (角色)     │     │  (角色)     │     │  (角色)     │
└─────────────┘     └─────────────┘     └─────────────┘
```

### 4.2 子对象审批流程

| 对象 | 审批链 | 电子签名要求 |
| --- | --- | --- |
| | | |

---

## 五、权限设计

### 5.1 角色与权限矩阵

| 角色 | 对象创建 | 对象编辑 | 流程发起 | 审批 | 只读 | 关闭 |
| --- | --- | --- | --- | --- | --- | --- |
| | ✓/✗ | ✓/✗ | ✓/✗ | ✓/✗ | ✓/✗ | ✓/✗ |

### 5.2 DAC（数据访问控制）

<!-- 描述数据可见性规则 -->


### 5.3 建议新增角色

| 角色名称 | 角色编码 | 说明 |
| --- | --- | --- |
| | `xxx` | |

---

## 六、编号规则汇总

| 对象 | 编号模式 | 示例 |
| --- | --- | --- |
| | | |

---

## 七、对象关系 ER 图

```
{主对象} (主对象)
├── 1:N → {子对象A} (关系说明)
├── 1:N → {子对象B} (关系说明)
└── 1:N → {子对象C} (关系说明)
```

---

## 附录：下拉值定义

**{字段名}（{字段编码}）**：
- 值1 — 说明
- 值2 — 说明

---

> **参考示例**：`d:/akso/akso-auto/external_reference_resources/AKSO_API_LIFECYCLE_CONFIG.md`（原料药生命周期闸门管理完整配置方案）
> 
> **填写指引**：
> - 对象清单、字段表 → 参考第二章
> - 生命周期设计 → 参考 2.2 节的状态图和流转规则
> - 菜单结构 → 参考 1.3 节的树形结构
> - 编号规则 → 参考第十五章
> - 权限矩阵 → 参考第十四章
> - ER 图 → 参考附录A
> - 工作流设计 → 参考第十三章
