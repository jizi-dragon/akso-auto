---
name: akso-gmp
display_name: "AksoGMP 配置编排器"
description: "Akso Gaia eGMP 配置编排器 — 负责配置前分析、流程编排决策、质量检查、工程化资源管理。通过配置蓝图驱动 akso-basic-config（DOM）和 akso-basic-config-api（API）两个执行 Skill 完成自动化配置。"
description_zh: "Akso Gaia eGMP 配置编排器：配置前分析 + 流程编排决策 + 质量检查 + 工程化资源管理"
skill_role: orchestrator
version: 4.0.0
updated: 2026-07-11
allowed-tools: Bash, Read, Write
agent_created: true
---

# AksoGMP 配置编排器

> **核心使命**：不直接操作浏览器，而是作为「配置大脑」——负责需求翻译、流程编排决策、质量检查、资源管理。底层 DOM/API 操作交给 akso-basic-config（DOM 版）和 akso-basic-config-api（API 版）两个执行 Skill。

## 零、三层架构总览

```
┌──────────────────────────────────────────────────────────────────┐
│                        编排层 (Orchestrator)                      │
│                     akso-gmp（本 Skill）                          │
│  配置前分析 · 流程编排决策 · 质量检查 · Playbook 管理 · 配置蓝图    │
├──────────────────────────────────────────────────────────────────┤
│                        执行层 (Executor)                          │
│  ┌─────────────────────────┐  ┌─────────────────────────────────┐│
│  │ akso-basic-config       │  │ akso-basic-config-api           ││
│  │ (DOM 版)                │  │ (API 版)                        ││
│  │ 浏览器 Playwright 操作  │  │ HTTP API 直调 (40x 提速)       ││
│  │ 可视化验证 · 页面探索   │  │ 批量配置 · Session 模式        ││
│  └─────────────────────────┘  └─────────────────────────────────┘│
├──────────────────────────────────────────────────────────────────┤
│                        复用层 (Reusable Assets)                   │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │ Playbooks (可回放配置流程)  ·  配置蓝图 (结构化配置文档)       ││
│  │ .learnings/ 踩坑记录        ·  编号规则模板 ·  权限矩阵模板   ││
│  └──────────────────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────────────┘
```

### Skill 协作流程

```
用户需求
   │
   ▼
┌─────────────────────┐
│ akso-gmp (编排层)    │
│ ① 分析需求，产出配置蓝图
│ ② 决策：用 DOM 版还是 API 版
│ ③ 分发蓝图到执行层
│ ④ 质量检查 & 结果汇总
└──────┬──────────────┘
       │  分发配置蓝图 (Markdown 结构化文档)
       ▼
┌──────────────────────┐    ┌──────────────────────┐
│ akso-basic-config    │    │ akso-basic-config-api │
│ (DOM)                │    │ (API)                │
│ 可视化创建/验证      │    │ 批量 API 调用        │
└──────┬──────────────┘    └──────┬──────────────┘
       │  执行结果摘要            │  执行结果摘要
       ▼                          ▼
┌─────────────────────┐
│ akso-gmp (编排层)    │
│ ⑤ 验证配置完整性
│ ⑥ 更新 Playbook
│ ⑦ 归档 .learnings/
└─────────────────────┘
```

### 各 Skill 职责速查

| Skill | 角色 | 一句话职责 |
|-------|------|-----------|
| **akso-gmp** | 编排者 | 翻译需求 → 产出蓝图 → 决策执行路径 → 质量把关 |
| **akso-basic-config** | DOM 执行者 | 浏览器自动化操作：登录、点击、填表、截图验证 |
| **akso-basic-config-api** | API 执行者 | HTTP API 直调：40x 批量创建对象/字段/布局/生命周期 |

## Skill 编排决策树

每次接到配置任务时，按以下决策树选择执行 Skill：

### 何时用 DOM 版（akso-basic-config）

- 需要**可视化验证**配置结果（截图确认）
- **首次探索**新页面/新功能（无现有 Playbook）
- 操作仅在 UI 上可行（如拖拽排序、富文本编辑、阶段组可视化配置）
- 需要验证登录态和 session 状态

### 何时用 API 版（akso-basic-config-api）

- 批量操作 **> 5 个**配置项（对象 + 字段 + 布局）
- 已建立 session（`x-csrf-token` 已知）
- **纯数据操作**：创建对象、字段、选项集、布局等
- 需要高吞吐（API 版比 DOM 版快约 40 倍）

### 何时混用（推荐默认模式）

```
登录 → DOM 版（akso-basic-config）
  │    验证 session、获取 CSRF token
  ▼
配置 → API 版（akso-basic-config-api）
  │    批量创建对象/字段/布局/生命周期
  ▼
验证 → DOM 版（akso-basic-config）
       截图确认、可视化检查
```

### 信息传递格式

```
配置蓝图 (Markdown 结构化文档)
  │
  │  由 akso-gmp 产出，传递给执行层 Skill
  │
  ▼
执行层 Skill → 解析蓝图 → 逐项执行 → 返回结果摘要
```

蓝图即「把业务需求翻译成 AKSO 配置语言」的中间产物。一份好的蓝图让执行层无需理解业务，只需逐项操作。

## 一、配置蓝图标准格式

配置蓝图是编排层的核心产出物，用于驱动执行层 Skill 完成自动化配置。一份完整的配置蓝图包含以下 7 个组成部分：

### 1. 对象清单表

列出本次配置涉及的所有对象（含类型、生命周期、电子签名、说明）。

| 序号 | 对象名称 | 对象编码 | 类型 | 生命周期 | 电子签名 | 说明 |
|------|----------|----------|------|----------|----------|------|
| 1 | xxx | xxx__c | 主对象 | ✓/✗ | ✓/✗ | 简要说明 |

### 2. 字段定义表

每个对象列出完整字段清单（含名称、编码、类型、必填、说明）。字段按「基本信息」「时间信息」「业务专项」「附件」等分组。

### 3. 生命周期状态图

ASCII 状态机图展示状态节点和流转条件，包含状态权限和电子签名要求。

### 4. 菜单结构图

树形结构展示业务端菜单层级，含默认视图和按角色筛选的子菜单。

### 5. 编号规则表

每个对象的编号模式（前缀 + 日期 + 流水号），含示例。

### 6. 权限矩阵

角色 × 操作矩阵，标注各角色对每个对象的 CRUD/审批/签名权限。

### 7. 对象关系 ER 图

ASCII 树形图展示对象间的 1:N 关系，明确父子/引用类型。

> 📘 **完整示例**：参考 `d:/akso/akso-auto/external_reference_resources/AKSO_API_LIFECYCLE_CONFIG.md`，这是一份按上述标准格式编写的「原料药生命周期闸门管理」配置蓝图，涵盖 14 个对象、50+ 字段、6 阶段生命周期、权限矩阵等全部 7 个组成部分。

> **核心定位**：蓝图就是「把业务需求翻译成 AKSO 配置语言」的中间产物。业务人员提供需求 → 编排层产出蓝图 → 执行层按蓝图操作。

## 学习目的

### 这个 Skill 存在的意义

你不是来听一堂 GMP 产品培训课的。这个 Skill 的设计目标是：

```
人类演示一次 → AI 观察理解 → 生成可复现的 Playbook → 下次 AI 独立完成
```

| 你的角色 | AI 的角色 |
|----------|-----------|
| 配置专家（做决策） | 学徒（观察、记录、推理、复现） |
| 演示操作路径 | 捕捉每一步：点了什么菜单、填了什么字段、选了哪个选项 |
| 解释「为什么这样配」 | 把「为什么」提炼成规则，绑定到 Playbook 中 |
| 纠正错误操作 | 自动记录踩坑经验，下次不犯同样的错 |

### 学习 ≠ 死记

AI 要学的不只是「点哪里」：

| 学什么（表面） | 为什么（深层） |
|---------------|---------------|
| 在对象创建页勾选「启用生命周期」 | 因为保存后不可修改，必须一开始就决定 |
| 把 Code 写为 `ChangeControl` | 因为 PascalCase 规范、关联对象前缀为 `object_` |
| 字段类型选「选项」而非「文本」 | 因为要保证数据标准化，方便后续筛选统计 |
| 创建选项字段前必须先创建选项集 | 因为字段创建时要选择数据来源，无选项集则无法完成 |
| 中间对象命名为 `变更关联的产品` | 因为多对多关系需要一个桥梁对象 |

### 学习完成后 AI 能做什么

- 你新接到一个 GMP 模块的配置需求 → AI 读取 Playbook 自动完成对象、字段、布局的创建
- 你说「按 CAPA 模块的方式配一个偏差模块」→ AI 理解 CAPA 的配置模式并映射到偏差
- 配置过程中踩了坑 → AI 自动记录到 `.learnings/`，下次规避
- 换了一个新环境 / 新系统 → 把 Skill 包和 Playbook 迁移过去，AI 立即恢复能力

---

## 配置工作流（核心）

### 总览

```
阶段 1          阶段 2          阶段 3          阶段 4
需求分析    →   对象建模    →   界面呈现    →   流程编排
(1-3天)         (1-2天)         (1-2天)         (2-4天)
    │               │               │               │
    ▼               ▼               ▼               ▼
阶段 5          阶段 6          阶段 7
权限落地    →   验证测试    →   持续优化
(1天)           (1-2天)         (持续)
```

### 阶段 1：需求分析 (Requirement Analysis)

**目标**：把业务需求翻译成配置语言。

| 步骤 | 操作 | 产出物 |
|------|------|--------|
| 1.1 梳理业务数据 | 在 Excel 中列出所有业务字段、字段类型、是否必填 | 字段草稿表 |
| 1.2 识别对象边界 | 确定哪些数据构成一个独立对象、哪些应拆分为子对象 | 对象清单 |
| 1.3 分析对象关系 | 画出对象关系图：父子/引用、1:1/1:N/N:M | 对象关系图 |
| 1.4 确定流程节点 | 梳理业务审批流：有哪些状态、谁能做什么操作 | 流程图草稿 |
| 1.5 整理人员权限 | 哪些角色可以看/可以改/可以审批 | 权限矩阵 |

**AI 的能力**：你告诉 AI 业务需求，AI 帮你检查 Checklist、提示遗漏。

### 阶段 2：对象建模 (Object Modeling)

**目标**：在 Gaia 平台中创建对象、字段、关系和选项集。

| 步骤 | 关键决策 |
|------|----------|
| 2.1 创建选项集 | 哪些字段用选项集而不是自由文本？ |
| 2.2 创建对象 | 是否启用生命周期？是否启用对象类型？ |
| 2.3 创建字段 | 用什么字段类型？默认值是什么？ |
| 2.4 建立关系 | 父子还是引用？需不需要中间对象？ |
| 2.5 设置编号规则 | 编号包含哪些信息（日期/序号/类型）？ |

> ⚠️ **前置依赖**：创建选项字段时，**必须先创建选项集（含选项值），再创建字段**。如果选项集尚未创建，字段创建时无法选择数据来源。

**AI 的能力**：学习模式下观察你的操作 → 生成 Playbook → 下次同类型对象自动配置。

### 阶段 3：界面呈现 (UI Presentation)

**目标**：让对象在用户界面上可见、可用。

| 步骤 | 关键决策 |
|------|----------|
| 3.1 列表布局 | 列表页显示哪些列？排序规则？ |
| 3.2 表单布局 | 字段怎么分组？哪些可折叠？ |
| 3.3 菜单配置 | 放在哪个菜单目录下？谁可见？ |
| 3.4 验证规则 | 什么条件下必填？值范围限制？ |

**AI 的能力**：按命名规范自动建议字段分组和菜单名称。

### 阶段 4：流程编排 (Process Orchestration)

**目标**：让对象按状态推进，自动触发审批和通知。

| 步骤 | 关键决策 |
|------|----------|
| 4.1 创建生命周期状态 | 有哪些状态？初始状态是什么？ |
| 4.2 设置进入条件/动作 | 什么条件下自动推进？ |
| 4.3 定义用户动作 | 提交/退回/取消，哪些是常用动作？ |
| 4.4 创建阶段组 | 如何让用户直观看到流程进度？ |
| 4.5 创建工作流 | 谁审批？判断条件是什么？ |
| 4.6 配置通知 | 什么时候通知谁？通知内容？ |
| 4.7 电子签名 | 哪些关键节点需要签名？ |

**AI 的能力**：理解生命周期状态机逻辑，自动推理状态转换的合理性。

### 阶段 5：权限落地 (Authorization)

**目标**：确保合适的人在合适入口执行合适动作。

| 步骤 | 关键决策 |
|------|----------|
| 5.1 创建角色 | 有哪些业务角色？ |
| 5.2 配置权限集 | 谁能创建？谁能查看？谁能编辑？ |
| 5.3 菜单权限 | 哪些角色能看到这个模块的菜单？ |
| 5.4 共享规则 | 跨部门的数据可见性怎么控制？ |

### 阶段 6：验证测试 (Validation)

**目标**：在业务端完整走通配置流程，确保无遗漏。

| 步骤 | 检验点 |
|------|--------|
| 6.1 业务端创建实例 | 菜单入口正确、表单字段完整、创建成功 |
| 6.2 走通完整流程 | 提交 → 审批 → 退回 → 重新提交，每个状态切换正确 |
| 6.3 权限验证 | 不同角色的用户登录，确认看到/看不到正确的内容 |
| 6.4 通知验证 | 确认审批人收到了通知（系统内/飞书） |
| 6.5 电子签名验证 | 关键节点签名功能正常 |

### 阶段 7：持续优化 (Continuous Improvement)

- 每次配置新模块时，AI 自动与已有 Playbook 交叉验证
- 踩坑自动记录到 `.learnings/`，形成知识沉淀
- 同类模块配置时间逐步降低

---

## 系统信息

- **平台名称**：Akso Gaia eGMP（腾邦生物）
- **环境清单**：环境信息（含登录地址、账号）通过外部 Excel 获取，详见 akso-basic-config Skill 第一章
- **配置入口**：**appstore → 管理端**（登录后默认在业务端首页，需点击顶部的 appstore/九宫格按钮，选择"管理端"进入）
- **⚠️ 导航关键**：登录后 → 点击顶部 `appstore` 按钮 → 选择 `管理端` → 左侧面板显示配置菜单（对象管理、业务逻辑、用户界面等）

### 一级模块索引

> 以下为配置端主要模块入口，具体路径和 URL 见 akso-basic-config Skill 的"页面快捷 URL"表。

| 模块分类 | 模块名 | 路径（相对） | 用途 |
|----------|--------|-------------|------|
| 对象管理 | 对象列表 | `/admin/config/basic-objects/list` | 创建/编辑对象 |
| 对象管理 | 选项集列表 | `/admin/config/pick-list/list` | 创建/编辑选项集 |
| 对象管理 | 编号规则 | `/admin/config/code-rules/list` | 编号规则配置 |
| 对象管理 | 联动规则 | `/admin/config/linkages-rule/list` | 字段联动 |
| 对象管理 | 查重规则 | `/admin/config/recurrence-check/list` | 查重规则 |
| 业务逻辑 | 对象生命周期 | `/admin/config/lifecycle` | 生命周期配置 |
| 业务逻辑 | 生命周期阶段组 | `/admin/config/stage-group` | 阶段组配置 |
| 业务逻辑 | 工作流 | `/admin/config/workflow` | 工作流配置 |
| 业务逻辑 | 工作流设置 | `/admin/config/workflow-setting` | 工作流全局设置 |
| 业务逻辑 | 审批矩阵类型 | `/admin/config/approval-matrix/list` | 审批矩阵 |
| 用户界面 | 菜单 | `/admin/config/menu-mgmt` | 菜单管理 |
| 用户界面 | 菜单集 | `/admin/config/menu-collection/list` | 菜单集 |
| 用户界面 | 视图 | `/admin/config/view-management` | 视图管理 |
| 用户界面 | 页面链接 | `/admin/config/page-link/list` | 页面链接 |
| 通知设置 | 消息模板 | `/admin/config/notice/list` | 消息模板 |

## 三种工作模式

| 模式 | 触发 | AI 行为 | 产出 |
|------|------|---------|------|
| **执行模式** | 直接下配置指令 | 加载 Playbook，自动操作 | 完成配置 + 截图 |
| **学习模式** | "我教你"、"看好了" | 观察操作 → 理解意图 → 记录路径 | 新 Playbook |
| **Playbook 模式** | "按上次的方法配" | 匹配 Playbook，套用执行 | 同类配置完成 |

---

## 核心配置概念（来自平台产品手册）

### 对象 (Object)
对象是业务实体的抽象。三种来源：系统（平台创建，不可改）、标准（开发组内置，Code 后缀 `__ak`）、自定义（配置人员创建，Code 后缀 `__c`，最多 500 个）。

**限制**：300 字段/对象、10 长文本+富文本、30 对象类型、100 万记录、20 关系。

### 对象关系
| 关系 | 含义 | 独立性 |
|------|------|--------|
| 父子 | 子不可脱离父 | 子对象依赖父对象 |
| 引用 | 各自独立存在 | 两个对象可独立 |

| 基数 | 含义 | 是否需要中间对象 |
|------|------|------------------|
| 多对一 | N 条记录对应 1 条 | 否 |
| 一对一 | 1 对 1 | 否（特殊多对多） |
| 多对多 | N 对 N | **是**（如变更↔产品 → 中间对象「变更关联的产品」） |

### 字段 (Field)
支持类型：文本、数字、布尔、日期时间、选项（绑定选项集）、长文本、富文本、对象（引用关系）、父对象（父子关系）。创建三步：选类型 → 输入名称/Code → 设选项。

### 生命周期 (Lifecycle)
状态机驱动业务流转。核心要素：状态、阶段组、进入条件、进入动作、用户动作。

### 工作流 (Workflow)
串联参与者、任务、判断、通知和电子签名的协同引擎。以角色作为参与人推荐方式。

---

## 命名规范速查

### Code 前缀（核心速查）

| 前缀 | 场景 | 说明 |
|------|------|------|
| `layout_` | 布局 | 表单/列表布局 |
| `part_` | 部分 | 表单中的字段分组 |
| `option_` | 选项集 | 字段下拉选项的数据来源 |
| `lifecycle_` | 生命周期 | 对象生命周期定义 |
| `workflow_` | 工作流 | 审批流程定义 |
| `stage_` / `status_` / `action_` | 阶段/节点/动作 | 生命周期三要素 |

> 完整 Code 前缀表见 akso-basic-config Skill 的"命名规范速查"。

### 中文命名规则
- 字段：`【部分】_【字段名】`（如 CAPA申请_审核人）
- 子对象：`【主对象】_【对象名】`（如 CAPA_行动措施）
- 生命周期：`【对象】_生命周期`
- 动作：`【状态】_【动作】`（如 开启_提交）

---

## 工程化资源管理

### 配置蓝图存放

所有配置蓝图统一存放在 `akso-gmp/playbooks/` 目录下，与可回放 Playbook 同目录管理：

```
akso-gmp/
├── SKILL.md
├── playbooks/
│   ├── blueprint-template.md          # 蓝图模板（空壳，按 7 组成部分填写）
│   ├── deviation-v1.0.md              # 偏差模块配置蓝图
│   ├── capa-v1.0.md                   # CAPA 模块配置蓝图
│   ├── api-lifecycle-v1.0.md          # 原料药生命周期配置蓝图
│   └── ...
└── .learnings/
```

### Playbook 命名规范

```
{模块名}-{版本}.md

示例：
  deviation-v1.0.md        # 偏差模块 v1.0
  capa-v2.1.md             # CAPA 模块 v2.1
  change-control-v1.0.md   # 变更控制 v1.0
```

### 蓝图模板

模板文件：`playbooks/blueprint-template.md`，新模块配置时从模板复制一份，按 7 个组成部分逐项填写。

### 版本管理建议

- 蓝图和 Playbook 均纳入 Git 版本管理
- 每次配置迭代更新版本号（主版本.次版本）
- 重大变更（新增对象/生命周期）→ 主版本 +1
- 小幅调整（字段增减/布局微调）→ 次版本 +1
- 提交信息格式：`[blueprint] 偏差模块 v1.0 初始配置蓝图`

---

## 跨平台迁移

### 可以自动化打包

**是的**，本 Skill 的设计使所有能力文件都可以一键打包导出。迁移后新平台上的 AI 只要加载这些文件，就能获得相同的能力。

### 打包命令

```bash
# 进入 skills 目录
cd ~/.workbuddy/skills

# 打包整个 akso-gmp skill（含所有 playbook）
tar -czf ~/Desktop/akso-gmp-skill-v3.0.0.tar.gz akso-gmp/

# 同时打包依赖的 Skills（如需要）
tar -czf ~/Desktop/akso-gmp-full-bundle.tar.gz \
  akso-gmp/ \
  playwright-browser-automation/ \
  self-improving-agent/
```

### 迁移清单

| 文件 | 路径 | 用途 |
|------|------|------|
| **akso-gmp Skill** | `~/.workbuddy/skills/akso-gmp/` | 核心配置大脑 |
| **Playbooks** | `~/.workbuddy/skills/akso-gmp/playbooks/` | 可复用配置流程 |
| **Playwright 自动化** | `~/.workbuddy/skills/playwright-browser-automation/` | 浏览器执行层 |
| **自我改进** | `~/.workbuddy/skills/self-improving-agent/` | 持续学习引擎 |
| **学习记录** | `d:/akso/akso_claw/.learnings/` | 踩坑和修正历史 |
| **MCP 配置** | `~/.workbuddy/mcp.json` | Playwright MCP 连接配置 |

### 新平台部署步骤

1. 将打包文件解压到新平台的 `~/.workbuddy/skills/` 目录
2. 将 `mcp.json` 合并到新平台的 MCP 配置
3. 复制 `.learnings/` 到新项目根目录
4. 安装 playwright-cli 和 @playwright/mcp
5. 重新加载 WorkBuddy → Skill 自动生效

---

## 已学模块索引

| 模块 | 类型 | Playbook | 状态 |
|------|------|----------|------|
| MAH 质量信息通知 2.0 | 业务功能演示（非配置） | `playbooks/mah-notice-use.md` | 已演示 |
| 基础配置流程验证 | 配置端全流程测试 | 见本文件 | 2026-06-03 完成 |

> 📘 配置端 Playbook 尚未创建。建议从「阶段 2：对象建模」开始录制第一个配置 Playbook。

## 知识来源

- 《AksoGaia 平台产品手册》v3.4.x（177 页）
- 《对象管理》功能说明（71 页）
- 《03.用词及命名规范》
- Gaia 学习资料 01-13（综合教学文档）

---

## 附录：与 AKSO_API_LIFECYCLE_CONFIG.md 的对照使用

`d:/akso/akso-auto/external_reference_resources/AKSO_API_LIFECYCLE_CONFIG.md` 是一份按本 Skill 定义的「配置蓝图标准格式」编写的完整示例。使用时按以下对照关系查阅：

| 蓝图组成部分 | AKSO_API_LIFECYCLE_CONFIG.md 对应章节 | 说明 |
|-------------|--------------------------------------|------|
| 1. 对象清单表 | 一、配置方案总览 → 1.2 配置对象清单 | 14 个对象的完整清单 |
| 2. 字段定义表 | 二～十二（各对象独立章节） | 每个子对象的完整字段定义 |
| 3. 生命周期状态图 | 2.2 生命周期设计、3.3 生命周期 | ASCII 状态机 + 流转规则 |
| 4. 菜单结构图 | 1.3 模块菜单结构 | 树形菜单层级 |
| 5. 编号规则表 | 十五、编号规则汇总 | 13 个对象的编号模式 |
| 6. 权限矩阵 | 十四、权限设计 → 14.1 角色与权限矩阵 | 7 角色 × 7 操作 |
| 7. 对象关系 ER 图 | 附录A | 1:N 树形关系图 |

### 使用建议

1. **作为模板参考**：新模块配置时，对照此蓝图的章节结构逐一填写
2. **交给执行层**：将此蓝图文件路径传给 akso-basic-config-api Skill，由其解析后批量创建
3. **质量检查**：配置完成后，回查蓝图确认所有对象/字段/生命周期均已创建
