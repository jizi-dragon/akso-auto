# Tasks

> 采用"用户演示 → AI 记录 → 复现验证"模式，每完成一步 DOM 记录后立即给出精确 Playwright selector。

## 阶段 1：生命周期状态基础 DOM 记录（交互式）

- [x] Task 1: 记录生命周期列表定位
  - [x] 1.1 用户演示：在 `/admin/config/lifecycle` 列表中如何找到目标对象的生命周期（搜索/筛选/直接浏览）
  - [x] 1.2 AI 记录：搜索框 selector (`getByPlaceholder('请输入查询条件')`)、表格列表 selector (`.ant-table-row`)、名称匹配规则（"对象名+生命周期"，需 `exact: true`）
  - [x] 1.3 AI 记录：点击进入生命周期详情页后的 URL 模式（`/admin/config/lifecycle/{uuid}?__edit=2`）

- [x] Task 2: 记录生命周期状态创建
  - [x] 2.1 用户演示：在生命周期详情页中"状态"表格上方的"创建"按钮点击
  - [x] 2.2 AI 记录：「创建」按钮的精确 Playwright selector（`button "创 建"`，需按表格区域定位）
  - [x] 2.3 AI 记录：创建表单中 name/Code/描述三个字段的精确 selector
  - [x] 2.4 AI 记录：保存按钮 selector 及保存后页面跳转行为
  - [x] 2.5 AI 记录：默认自带状态（"开启" init_status__c / "关闭" complete_status__c）的列表结构

- [x] Task 3: 记录状态详情页进入方式
  - [x] 3.1 用户演示：从状态列表中点击某个状态进入详情页
  - [x] 3.2 AI 记录：状态行 click 的精确 selector、详情页 URL 模式（`/admin/config/lifecycle/{lifecycleUuid}/status/{statusUuid}?__edit=2`）
  - [x] 3.3 AI 记录：详情页中 11 个折叠 section 结构（进入条件/进入动作/用户动作 + 6 类状态权限）

## 阶段 2：三个复杂子模块 DOM 记录（用户逐模块演示）

- [x] Task 4: 记录「进入条件」创建
  - [x] 4.1 用户演示：点击「进入条件」→「创建」→ 表单填写 → 保存
  - [x] 4.2 AI 记录：创建按钮 selector、表单字段、执行方式（总是执行/有条件地执行）、执行条件
  - [x] 4.3 AI 记录：创建后列表项结构（执行方式 | 执行条件 | 操作）和可点击链接

- [x] Task 5: 记录「进入动作」创建
  - [x] 5.1 用户演示：点击「进入动作」→「创建」→ 选择动作类型 → 填写参数 → 保存
  - [x] 5.2 AI 记录：动作类型选择器 selector、表单字段（描述/规则/动作类型/状态）
  - [x] 5.3 AI 记录：主要动作类型（发送通知/更新字段/创建记录/触发Web动作）的表单差异

- [x] Task 6: 记录「用户动作」创建
  - [x] 6.1 用户演示：点击「用户动作」→「创建」→ 填写名称/Code/目标状态等 → 保存
  - [x] 6.2 AI 记录：表单字段 selector（描述/动作名称/Code/图标/分组/动作类型）、entry-action 子项结构

## 阶段 3：SKILL.md 升级

- [x] Task 7: 重写 SKILL.md 第七章基于记录的 DOM 信息
  - [x] 7.1 重写 7.1「进入生命周期」— 补全列表定位（搜索→精确匹配→点击进入）
  - [x] 7.2 重写 7.2「创建状态」— 用实际 UI 替换画布错误描述
  - [x] 7.3 在 7.2 末尾新增 7.2.2「进入状态详情页」小节
  - [x] 7.4 重写 7.4「用户动作」— 改为状态详情页内配置 + entry-action 子项
  - [x] 7.5 重写 7.6「进入条件」— 改为状态详情页内配置
  - [x] 7.6 重写 7.7「进入动作」— 改为状态详情页内配置
  - [x] 7.7 7.3 状态连线 — 添加注释标注待实际验证
  - [x] 7.8 更新附录 B 新增 B.17 生命周期踩坑经验
  - [x] 7.9 更新版本号 v5.2→v5.3 + 日期

# Task Dependencies
- Task 2 依赖 Task 1（需要先定位到生命周期详情页）
- Task 3 依赖 Task 2（需要先有已创建的状态才能点击进入）
- Task 4/5/6 依赖 Task 3（需要在状态详情页中操作三个 section）
- Task 7 依赖 Task 1~6 全部完成（汇总所有 DOM 记录）
