# Tasks

- [x] Task 1: 重构 akso-gmp SKILL.md — 精简为编排者角色
  - [x] 1.1 在 front-matter 中新增 `skill_role: orchestrator` 字段
  - [x] 1.2 修改 description 体现编排职责
  - [x] 1.3 新增「零、三层架构总览」章节，用决策树和流程图定义三 Skill 的协作关系
  - [x] 1.4 新增「一、配置蓝图标准格式」章节，定义蓝图模板（字段表、生命周期图、权限矩阵的结构），引用 `AKSO_API_LIFECYCLE_CONFIG.md` 作为示例
  - [x] 1.5 保留并精简「配置工作流 7 阶段」概览（移除底层操作细节，只留决策要点）
  - [x] 1.6 保留「核心配置概念」和「三种工作模式」
  - [x] 1.7 移除「浏览器操作规范」整章（归入 DOM 版 Skill）
  - [x] 1.8 移除「登录页 DOM 精确定位」表格（归入 DOM 版 Skill）
  - [x] 1.9 移除「页面快捷 URL」详情表，改为一级模块索引（仅路径/用途，不含 Playwright 命令）
  - [x] 1.10 移除「命名规范速查」完整表，改为仅保留前缀含义速查
  - [x] 1.11 移除「系统信息」中的密码和账号（仅保留环境名称和登录地址）
  - [x] 1.12 新增「工程化资源管理」章节：配置蓝图的存放位置、Playbook 命名规范、版本管理建议
  - [x] 1.13 新增 Skill 编排决策树：何时用 DOM 版、何时用 API 版、何时两者混用
  - [x] 1.14 附录新增：与 `AKSO_API_LIFECYCLE_CONFIG.md` 的对照使用说明

- [x] Task 2: 重构 akso-basic-config SKILL.md — DOM 执行指令集统一格式
  - [x] 2.1 在 front-matter 中新增 `skill_role: executor_dom` 字段
  - [x] 2.2 调整章节编号体系：零→快速定位、一→环境与登录、二→选项集、三→对象、四→字段、五→表单布局、六→列表布局、七→生命周期（扩展）、八→工作流（扩展）、九→菜单与权限（扩展）、十→验证测试、附录A→命名规范、附录B→踩坑经验
  - [x] 2.3 重写「第七章 生命周期」：在现有「创建状态」基础上补全以下 DOM 操作步骤——状态连线（拖拽/点击连线）、用户动作添加（动作名称/Code/目标状态）、阶段组创建、进入条件设置
  - [x] 2.4 重写「第八章 工作流」：在现有概要基础上补全参与者节点定位、任务节点配置、判断条件设置、通知节点配置、电子签名节点、节点连线操作、保存激活
  - [x] 2.5 扩展「第九章 菜单与权限」：菜单部分已有，补全权限集创建、对象级权限配置、字段级安全
  - [x] 2.6 将现有的操作步骤统一为「目标 → 前置条件 → 关键定位 → 操作步骤 → 验证方法」五要素结构
  - [x] 2.7 确保现有 scripts/ 和 examples/ 目录的文件引用与新章节编号一致
  - [x] 2.8 更新 CHANGELOG.md 记录本次重构变更

- [x] Task 3: 重构 akso-basic-config-api SKILL.md — API 执行指令集统一格式
  - [x] 3.1 在 front-matter 中新增 `skill_role: executor_api` 字段
  - [x] 3.2 调整章节编号与 DOM 版对齐：零→执行架构、一→认证、二→对象、三→选项集、四→字段、五→表单布局、六→列表布局、七→生命周期（扩展）、八→工作流（扩展+API 探索）、九→菜单与权限（API 探索）、十→执行策略
  - [x] 3.3 重写「第七章 生命周期」：在现有 `Status/Create` 基础上，将 2.8 节 8 个已捕获查询 API 升级为正式文档（标注「待验证」），新增状态连线 API 探索任务路标
  - [x] 3.4 新增「第八章 工作流」：标注当前工作流 API 为「待探索」，给出已捕获的 API 端点路标（如有），引导 Agent 在实时会话中通过浏览器 Network 面板录制
  - [x] 3.5 扩展「第九章 菜单与权限」：标注为「待探索」，给出菜单 API 的已知路径（`MenuGroup/QueryList`），引导 Agent 录制
  - [x] 3.6 更新执行策略对比表：将每个配置项的 DOM/API 成熟度标记为「✅ 已支持」「⚠️ 待验证」「❌ 待探索」三级
  - [x] 3.7 更新「能力边界」表，对齐新的覆盖范围
  - [x] 3.8 更新 `examples/full-workflow.md` 中的示例代码与新章节编号一致

- [x] Task 4: 新增配置蓝图模板文件
  - [x] 4.1 在 `akso-gmp/playbooks/` 下创建 `blueprint-template.md`，包含完整的配置蓝图模板（对象清单表、字段定义表、生命周期状态图、菜单结构图、编号规则表、权限矩阵、对象关系 ER 图）
  - [x] 4.2 模板中标注每个章节的填写指引和参考示例链接（指向 `AKSO_API_LIFECYCLE_CONFIG.md`）
  - [x] 4.3 模板采用 Markdown 表格 + ASCII 图表格式，确保纯文本可读

# Task Dependencies
- Task 2 和 Task 3 可并行执行（无相互依赖）
- Task 1 依赖 Task 2 和 Task 3 的章节编号确定后，才能精确编写编排决策树中的引用
- Task 4 依赖 Task 1 的蓝图格式定义完成后编写
- 建议执行顺序：Task 2 + Task 3（并行）→ Task 1 → Task 4
